version: "3.9"

services:
  parabens:
    build: .
    image: parabens-vc:latest
    ports:
      - "8080:8080"
    environment:
      SHORTLINK_DB: /app/data/shortlinks.sqlite
      PUBLIC_BASE_URL: https://parabens.vc
    volumes:
      - /home/renatoc/parabens-vc/data:/app/data
    restart: unless-stopped
